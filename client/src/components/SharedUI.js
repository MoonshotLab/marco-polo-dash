import styled from 'styled-components';

const StyledButtonList = styled.ul`
  list-style: none;
`;

const StyledButtonListItem = styled.li`
  display: inline-block;
  margin-right: 1em;
`;

export { StyledButtonList, StyledButtonListItem };
